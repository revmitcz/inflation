var range = [1665, 2013];
var pairwise_start = 1800;

templates = [
  'in-%Y1-dollars',
  '%Y1-dollars-in-%Y2-dollars'
];

for (var i=range[0]; i < range[1]; i++) {
  var year = i;
  for (var j=pairwise_start; j <= range[1]; j++) {
    var otheryear = j;
  }
}
